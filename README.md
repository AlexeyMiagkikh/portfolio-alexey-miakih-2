🌐 Portfolio — Alexey Miagkikh

Одностраничное портфолио с упором на скорость, чистый UI и понятный код.
Сайт написан на HTML + CSS + чистом JavaScript без зависимостей. Поддерживает тёмную/светлую темы, адаптивную навигацию и сохраняет имя пользователя в localStorage.

Демо (GitHub Pages): https://<твой-логин>.github.io/<имя-репозитория>/
(После публикации на GitHub Pages подставь реальную ссылку.)

✨ Ключевые возможности

Адаптивная шапка (sticky) с блюром и полупрозрачным фоном.

Бургер-меню для мобильных устройств:

Открытие/закрытие по клику.

Закрытие по Esc и по клику вне меню.

Автозакрытие при переходе по ссылке.

Светлая/тёмная тема:

Автоопределение по системной настройке (prefers-color-scheme).

Сохраняется в localStorage.

Доступная кнопка с корректными aria-* и сменой эмодзи/лейбла.

Приветствие с именем:

Поле ввода + «Сохранить»/«Сброс».

Имя капитализируется и сохраняется в localStorage.

Мгновенный предпросмотр фразы «Здравствуйте, …! 👋».

Гладкая прокрутка и возврат на верх по клику на логотип.

Герой-секция split-layout со скошенным правым фоном.

Галерея проектов — плитки с лёгкой подсветкой/градиентом.

Форма контактов (статическая) — без бэкенда, как визуальный элемент.

Резервная картинка для аватара через onerror.

Токены темы через CSS-переменные: цвета, тени, скругления, контейнер.

🧱 Технологии

HTML5 (семантика, aria-атрибуты)

CSS3 (custom properties, responsive grid/flex, эффекты)

Vanilla JS (без библиотек)

Шрифт Inter с Google Fonts

🗂️ Структура проекта
/ (корень)
├── index.html      # разметка и секции (Hero, About, Skills, Projects, Contact)
├── styles.css      # дизайн-система на CSS-переменных + адаптивные стили
├── script.js       # тема, навигация, приветствие, UX-поведение
└── README.md       # этот файл

🚀 Быстрый старт

Локально

Склонируй репозиторий:

git clone https://github.com/<твой-логин>/<имя-репозитория>.git
cd <имя-репозитория>


Открой index.html двойным кликом или через Live Server (VS Code).

Публикация на GitHub Pages

Зайди в Settings → Pages.

В разделе Build and deployment:

Source: Deploy from a branch

Branch: main, папка / (root)

Сохрани — через минуту появится ссылка вида:
https://<твой-логин>.github.io/<имя-репозитория>/

Если репозиторий не «user/organization site», а «project site», пути к ассетам уже корректные (относительные). Ничего менять не нужно.

♿ Доступность (A11y)

Навигационные элементы снабжены aria-label, aria-controls, aria-expanded.

Кнопка темы переключает aria-pressed и текстовую метку для скринридеров.

Закрытие мобильного меню по Esc и клику вне меню.

Контраст и фокус-состояния соответствуют теме.
(При желании можно добавить явные focus-outline для ссылок/кнопок.)

🔍 SEO и мета

<meta name="description"> присутствует.

Семантические заголовки (h1, h2) и логичная иерархия секций.

Человекочитаемые тексты в ссылках («Обо мне», «Навыки», …).

⚙️ Архитектура и поведение
Тема

При загрузке:

Берётся сохранённая тема из localStorage.

Иначе — системная настройка (light/dark).

Тема хранится в атрибуте data-theme на <html>.

Переключатель синхронизирует и emoji, и aria-метки.

Навигация

Состояние меню — класс .open на .nav.

В мобильном режиме ссылки показываются в выпадающем блоке.

Закрытие:

По клику на пункт меню.

По клику вне навигации.

По клавише Escape.

Приветствие

Ключ localStorage: username.

Капитализация имени и живой предпросмотр.

Кнопки Сохранить / Сброс + сохранение по Enter и blur.

📏 Стандарты кода

CSS-токены: --bg, --surface, --card, --text, --muted, --accent, --radius и т. д.

JS-модули не используются (один файл, простая инициализация).

Минимум «магических чисел», говорящие имена функций (setOpen, setTheme, persist, render).

🛠️ Как изменить под себя

Цветовую схему — правь токены в :root и [data-theme="light"].

Секции — добавляй новые карточки проектов в #projects.

Фото — замени src в блоке .hero-photo (резервная ссылка останется).

✅ Чек-лист качества

 Адаптив: ≥320px ширина, гриды/флексы.

 Тёмная/светлая тема + системная автонастройка.

 A11y: aria-атрибуты, Esc, клик вне меню.

 Нет внешних JS-зависимостей (мгновенная загрузка).

 Стабильные относительные пути (готово для GitHub Pages).

🗺️ Roadmap / Идеи на будущее

Функционал

 Отдельная страница проекта (case-study) с описанием задач и стеком.

 Фильтры/теги проектов (JS: фильтрация плиток).

 Лёгкая анимация появления секций при скролле (IntersectionObserver).

 Кнопка «Наверх» (scroll-to-top) после 400–600px прокрутки.

 Кеширование выбранной секции в URL hash + подсветка активного пункта меню.

Контакты

 Подключение формы к сервису (Formspree / Netlify Forms) или простому бекенду (Spring Boot).

 Валидация инпутов + сообщения об ошибках/успехе.

Производительность

 Ленивая загрузка изображений (loading="lazy") и адаптивные srcset.

 Иконочный шрифт/спрайт вместо эмодзи (по желанию).

 Минификация CSS/JS (build-скрипт или GitHub Action).

Доступность и UX

 Явимый :focus-visible для ссылок/кнопок.

 Сохранение последней открытой темы в localStorage уже есть — добавить «Системная» как третий режим.

 Поддержка нескольких языков (ru/en) через атрибуты lang + переключатель.

SEO/мета

 Open Graph / Twitter Card (og:title, og:description, og:image).

 Sitemap и robots.txt.

🧪 Тестирование (рекомендации)

Lighthouse в Chrome: целевые показатели 90+ (Performance/Best Practices/SEO).

Тест клавиатурой: Tab/Shift+Tab по всем интерактивным элементам, Esc закрывает меню.

Проверка на реальных мобильных ширинах: 360, 390, 414, 768, 1024, 1440.

📄 Лицензия

Укажи желаемую: MIT / Apache-2.0 / «Все права защищены».
Пример MIT: создай файл LICENSE и вставь шаблон MIT с твоим именем и годом.

📬 Контакты

Телефон: +996 555 35 35 25

Email: m.alex2018@mail.ru

GitHub: https://github.com/AlexeyMiagkikh
